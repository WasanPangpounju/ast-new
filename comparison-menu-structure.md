# เปรียบเทียบโครงสร้างเมนู/Navigation ระบบ "วัตถุดิบ": Laravel เดิม vs Next.js ใหม่

> เอกสารนี้สร้างจากการอ่านโค้ดจริง (read-only) ในทั้งสองโปรเจกต์ ณ วันที่ 2026-07-10
> - โปรเจกต์เดิม: `C:\Users\Friendly Dev\project\ast-laravel-clean`
> - โปรเจกต์ใหม่: `C:\Users\Friendly Dev\project\ast-new`
>
> ดูเอกสารคู่กัน: `comparison-raw-material.md` (เปรียบเทียบโครงสร้างข้อมูล/business logic)

---

## 1. ตำแหน่งเมนู "วัตถุดิบ" ใน Sidebar

| | Laravel เดิม | Next.js ใหม่ |
|---|---|---|
| ไฟล์ที่กำหนดเมนู | `resources/views/layouts/astmanufacturing.blade.php` (บรรทัด 216-416) | `src/app/(dashboard)/NavLinks.tsx` (บรรทัด 19-233) + `SidebarLayout.tsx` (chrome) + `src/lib/menus.ts` (permission list แยกต่างหาก) |
| ระดับเมนู | Top-level "วัตถุดิบ" (icon `fa-shopping-cart`) — เป็น sibling กับ "ระบบคลังสินค้า" | Top-level "ระบบวัตถุดิบ" — เป็น sibling กับ "ระบบคลังสินค้า" เช่นกัน |
| ลำดับใน sidebar | หน้าหลัก → ระบบซื้อขาย → **ระบบคลังสินค้า** → ระบบผลิตสินค้า (ตายแล้ว, href="#") → **วัตถุดิบ** → ผู้ตรวจสอบ → ตั้งค่า | หน้าหลัก → ระบบซื้อขาย → **ระบบคลังสินค้า** → **ระบบวัตถุดิบ** → ตั้งค่า |
| ตำแหน่งสัมพัทธ์ | วัตถุดิบอยู่ห่างจากคลังสินค้า 1 เมนู (มี "ระบบผลิตสินค้า" คั่นซึ่งเป็นเมนูตาย) | วัตถุดิบอยู่**ติดกันทันที**หลังคลังสินค้า — ไม่มีเมนูตายคั่น |

**สรุป**: ทั้งสองระบบจัดวัตถุดิบเป็น top-level menu แยกจากคลังสินค้าผ้าเหมือนกัน ไม่ได้ซ้อนเป็น submenu ของกันและกัน — ระบบใหม่จัดกลุ่มได้ **สะอาดกว่า** เพราะตัดเมนูตาย "ระบบผลิตสินค้า" ที่คั่นอยู่เดิมออกไป

---

## 2. รายการเมนูย่อยของ "วัตถุดิบ" — เปรียบเทียบทีละรายการ

| ลำดับเดิม | เมนูย่อยเดิม (label) | Route เดิม | ลำดับใหม่ | เมนูย่อยใหม่ (label) | Route ใหม่ | เปลี่ยนชื่อ/ย้ายหรือไม่ |
|---|---|---|---|---|---|---|
| 1 | ตรวจสอบนำเข้าวัตถุดิบ | `material.index` (`/material`) | 2 | ประวัติการนำเข้า | `/warehouse/material/history` | **เปลี่ยนชื่อ** "ตรวจสอบ" → "ประวัติ" |
| 2 | ตรวจสอบเบิกวัตถุดิบออกภายใน | `materialstore.index` | 4 | ประวัติการเบิก | `/warehouse/material/requisition-history` | เปลี่ยนชื่อเช่นกัน |
| 3 | ตรวจสอบเบิกวัตถุดิบออกภายนอก | `materialoutside.index` | 6 | ประวัติเบิกภายนอก | `/warehouse/material/outside-history` | เปลี่ยนชื่อเช่นกัน |
| 4 | นำเข้าวัตถุดิบ | `material.create` | 1 | นำเข้าวัตถุดิบ | `/warehouse/material/create` | **ชื่อเดียวกัน** |
| 5 | เบิกวัตถุดิบออกภายนอก | `materialoutside.create` | 5 | เบิกวัตถุดิบภายนอก | `/warehouse/material/outside` | ตัดคำว่า "ออก" ออก แต่ความหมายเดิม |
| 6 | เบิกวัตถุดิบออกภายใน | `materialstore.create` | 3 | เบิกวัตถุดิบภายใน | `/warehouse/material/requisition` | ตัดคำว่า "ออก" ออก เช่นกัน |
| 7 | คืนบรรจุภัณฑ์ | `package.index` (`/package`) | 8 | ส่งคืนบรรจุภัณฑ์ | `/warehouse/returns` | ⚠️ **ดูข้อ 3 ด้านล่าง — ชื่อคล้ายแต่ปลายทางต่างกัน** |
| — | (อยู่นอกกลุ่มวัตถุดิบ ใน "ผู้ตรวจสอบ") สต๊อกวัตถุดิบ | `materialstock.index` | 7 | สต็อกวัตถุดิบ | `/warehouse/material/stock` | **ย้ายเข้ามาอยู่ในกลุ่มวัตถุดิบ** (ดูข้อ 4) |
| — | ไม่มี | — | — | ❌ ไม่มีเมนู | `/api/warehouse/material/return` (API เท่านั้น) | **ฟีเจอร์ใหม่ "คืนวัตถุดิบเข้าสต็อก" ไม่มี UI เลย** (ดูข้อ 5) |

**การเปลี่ยนแปลงลำดับที่สำคัญ**:
- ระบบเดิม: จัดกลุ่ม "ตรวจสอบ" (ดูรายการ/history) ไว้ **ก่อน** "สร้างรายการใหม่" (create) ทั้ง 3 คู่ (index มาก่อน create เสมอในโค้ด sidebar)
- ระบบใหม่: จัดกลุ่มแบบ **create นำ, history ตาม** เป็นคู่ๆ ต่อเนื่องกัน (นำเข้า→ประวัตินำเข้า, เบิกภายใน→ประวัติเบิก, เบิกภายนอก→ประวัติเบิกภายนอก) — ระบบใหม่จัดกลุ่มตามลำดับ workflow ของผู้ใช้ (ทำก่อน แล้วดูประวัติทีหลัง) ซึ่ง**อ่านง่ายและ intuitive กว่า**ระบบเดิมที่ผสมกันไม่เป็นคู่

---

## 3. ⚠️ ข้อควรระวัง: "คืนบรรจุภัณฑ์" ชื่อคล้ายกันแต่เป็นคนละฟีเจอร์

- **ระบบเดิม**: "คืนบรรจุภัณฑ์" → `package.index` เป็นฟีเจอร์จริงสำหรับติดตามการคืนบรรจุภัณฑ์ (spool/sack/box/pallet) ให้ซัพพลายเออร์ ผูกกับตาราง `packages`/`packageasts` ที่สร้างขึนตอนรับ/ส่งวัตถุดิบ (ตามที่พบใน `comparison-raw-material.md` หัวข้อ 4)
- **ระบบใหม่**: "ส่งคืนบรรจุภัณฑ์" → `/warehouse/returns` ชี้ไปที่ `FabricReturnPage.tsx` ซึ่งเป็นหน้า**คืนผ้า (fabric)** ไม่ใช่หน้าคืนบรรจุภัณฑ์วัตถุดิบ — เป็นการ**นำ label เดิมมาแปะกับฟีเจอร์คนละโดเมน** (คลังผ้า ไม่ใช่วัตถุดิบ)
- ผลคือ: **ฟีเจอร์ "คืนบรรจุภัณฑ์วัตถุดิบ" ของเดิมหายไปจากเมนูวัตถุดิบใหม่โดยสิ้นเชิง** ผู้ใช้ที่คุ้นเคยกับระบบเดิมและคลิก "ส่งคืนบรรจุภัณฑ์" ในเมนูวัตถุดิบใหม่จะ**ไปเจอหน้าคืนผ้าแทน** ซึ่งอาจสร้างความสับสนหรือทำให้เข้าใจผิดว่าฟีเจอร์นี้ยังทำงานอยู่ทั้งที่จริงๆ ไปคนละที่

---

## 4. การย้ายเมนู "สต็อกวัตถุดิบ" เข้ามาในกลุ่มวัตถุดิบ (ปรับปรุงดีขึ้น แต่มี bug ใหม่)

- **ระบบเดิม**: "สต๊อกวัตถุดิบ" (`materialstock.index`) ถูกจัดไว้ใน top-level menu แยกชื่อ **"ผู้ตรวจสอบ"** (Inspector/Auditor) ซึ่งอยู่คั่นระหว่างเมนู "วัตถุดิบ" กับ "ตั้งค่า" — ผู้ใช้ที่ต้องการดูสต็อกต้องรู้ว่าไม่ได้อยู่ในเมนูวัตถุดิบ ต้องไปหาที่เมนูอื่น (ใช้ 3 คลิก: ขยายเมนู "ผู้ตรวจสอบ" → เข้าเพจ → ค้นหา)
- **ระบบใหม่**: ✅ ย้าย "สต็อกวัตถุดิบ" เข้ามาอยู่**ในกลุ่ม "ระบบวัตถุดิบ" โดยตรง** (ลำดับที่ 7 ของ submenu) — เป็นการจัดกลุ่มที่ดีขึ้นตาม mental model ที่ถูกต้อง (สต็อกเป็นส่วนหนึ่งของวัตถุดิบ)
- ⚠️ **แต่พบ bug ใหม่ที่ทำลายข้อดีนี้**: ไฟล์ `src/lib/menus.ts` (`ALL_MENUS`, บรรทัด 29-35) ซึ่งใช้สร้างรายการสิทธิ์ในหน้า admin จัดการผู้ใช้ (`admin/users/page.tsx`) **ไม่มี key `material.stock` อยู่ในรายการ** — มีแค่ 6 keys (create, history, requisition, requisition-history, outside, outside-history) ขาด stock ไป เนื่องจากสิทธิ์ของผู้ใช้ที่ไม่ใช่ admin (`allowedMenuKeys`) ถูกกำหนดผ่านหน้านี้ **ผู้ใช้ที่ไม่ใช่ admin จะไม่มีทางถูกให้สิทธิ์เข้าดู "สต็อกวัตถุดิบ" ได้เลย** (เห็นเมนูนี้เฉพาะ admin ที่ `allowedMenuKeys` เป็น null เท่านั้น) — เป็น **regression ที่ทำให้ฟีเจอร์เข้าถึงยากกว่าจงใจ** ทั้งที่ตำแหน่งเมนูดีขึ้นแล้ว

---

## 5. ⚠️ ฟีเจอร์ใหม่ "คืนวัตถุดิบเข้าสต็อก" (MaterialReturn) — มี Backend แต่ไม่มี UI เลย

จากเอกสาร `comparison-raw-material.md` เคยระบุว่า `MaterialReturn` เป็นฟีเจอร์ใหม่ที่ระบบเดิมไม่มี แต่จากการตรวจสอบ navigation/menu รอบนี้พบเพิ่มเติมว่า:

- API `POST/GET /api/warehouse/material/return` มีอยู่จริงและทำงานได้ (`src/app/api/warehouse/material/return/route.ts`)
- **ไม่มี `page.tsx` ไม่มี form component ไม่มี sidebar entry ใดๆ เลย** เชื่อมโยงไปยัง endpoint นี้ (ยืนยันด้วย `git log --diff-filter=A` — มีแค่การสร้างไฟล์ API เพียงไฟล์เดียว ไม่เคยมีการสร้างหน้า UI คู่กัน)
- **สรุป: ผู้ใช้ทั่วไปไม่มีทาง "คืนวัตถุดิบเข้าสต็อก" ผ่านหน้าจอได้เลยในปัจจุบัน** ต้องเรียก API ตรงๆ (เช่นผ่าน Postman) เท่านั้น — เป็น**ฟีเจอร์ที่ยังสร้างไม่เสร็จ** ไม่ใช่แค่ "ซ่อนอยู่ลึก" แต่ "เข้าถึงไม่ได้เลย" จาก UI

เทียบกับฟีเจอร์อื่นในกลุ่มเดียวกันที่ครบทั้ง form + history + สรุปสต็อก (นำเข้า, เบิกภายใน, เบิกภายนอก) — "คืนวัตถุดิบ" เป็นจุดที่ยังไม่ตามงานอื่นทัน

---

## 6. จำนวนคลิก/ขั้นตอนเพื่อไปถึงฟังก์ชันเดียวกัน

| ฟังก์ชัน | Laravel เดิม | Next.js ใหม่ | ต่างกันอย่างไร |
|---|---|---|---|
| รับวัตถุดิบเข้า | 1) ขยาย "วัตถุดิบ" 2) คลิก "นำเข้าวัตถุดิบ" 3) กรอกฟอร์ม→คลิก "ตรวจสอบ" (ไป confirm page) 4) คลิก "บันทึก" — **รวม 4 คลิก + 2 หน้า (wizard 2 ขั้นตอน: กรอก→ยืนยัน→บันทึก)** | 1) ขยาย "ระบบวัตถุดิบ" 2) คลิก "นำเข้าวัตถุดิบ" 3) กรอกฟอร์ม→คลิก "บันทึก" (บันทึกทันที ไม่มีหน้ายืนยัน) — **รวม 3 คลิก + 1 หน้า** | ระบบใหม่ **ตัดขั้นตอนยืนยัน (confirm page) ออก** ลดจำนวนคลิก/หน้าลง แต่ก็ตัดโอกาสให้ผู้ใช้ตรวจทานข้อมูลก่อนบันทึกจริงไปด้วย (trade-off ความเร็ว vs ความปลอดภัยจากการกรอกผิด) |
| เบิกใช้ภายใน | 4 คลิก + 2 หน้า (รูปแบบเดียวกับข้างบน, ไปหน้า `checkwithdraw` ก่อนกด "เบิกวัตถุดิบ") บันทึกทีละฟอร์ม (แต่ในฟอร์มเดียวมีได้หลาย row ผ่าน JS เพิ่มแถว) | 2 คลิกเข้าเพจ + กรอก + "เพิ่มรายการใหม่" (เก็บใน local pending list ไม่ยิง API) + "บันทึก N รายการ" (ยิง API ทีเดียวทั้งชุด) — **ไม่มีหน้ายืนยันแยก แต่มี pending-list บนหน้าเดียวกันแทน** | รูปแบบ multi-row เหมือนกันในหลักการ (ทั้งคู่รองรับเบิกหลายรายการในครั้งเดียว) แต่ระบบใหม่รวม preview + บันทึกไว้หน้าเดียว ไม่ต้อง navigate ไปหน้าอื่น |
| เบิกออกภายนอก | เหมือนรูปแบบ "เบิกใช้ภายใน" เดิม — 4 คลิก + 2 หน้า, บันทึกทีละ record ไม่รองรับหลายแถวในฟอร์มเดียว (เท่าที่ agent รายงาน) | เหมือนรูปแบบ requisition ใหม่ — batch add + บันทึกครั้งเดียว, มี field เพิ่ม (ผู้รับ/การนำไปใช้/หมายเหตุการเงิน) ที่ไม่มีในเบิกภายใน | ระบบใหม่ทำให้ "เบิกภายนอก" รองรับหลายแถวต่อครั้งได้เหมือน "เบิกภายใน" ซึ่งเดิมทั้งสองฟอร์มมีพฤติกรรมต่างกัน |
| ดูสต็อกคงเหลือ | 3 คลิก แต่ต้องรู้ว่าอยู่เมนู **"ผู้ตรวจสอบ"** ไม่ใช่เมนู "วัตถุดิบ" (สร้างความสับสนเชิง discoverability) | 2 คลิก และอยู่**ในเมนูวัตถุดิบโดยตรง** — แต่ (ดูข้อ 4) ใช้ได้เฉพาะ admin เท่านั้นในทางปฏิบัติ | ตำแหน่งดีขึ้นสำหรับ admin, แย่ลงสำหรับผู้ใช้ทั่วไป (เข้าถึงไม่ได้เลยเพราะ permission bug) |
| คืนวัตถุดิบเข้าสต็อก (ฟีเจอร์ใหม่) | ไม่มีฟีเจอร์นี้ในระบบเดิม | **0 คลิก — ไปไม่ถึงเลยเพราะไม่มี UI** | ดูข้อ 5 |
| คืนบรรจุภัณฑ์ให้ซัพพลายเออร์ | มีฟีเจอร์จริง เข้าถึงได้ผ่านเมนู "คืนบรรจุภัณฑ์" | เมนูชื่อคล้ายกันแต่ไปหน้าคืนผ้าแทน — **ฟีเจอร์จริงเข้าถึงไม่ได้** | ดูข้อ 3 |

---

## 7. การจัดกลุ่ม Field ในฟอร์ม (นำเข้าวัตถุดิบ) — เปรียบเทียบ

โครงสร้างฟอร์ม "นำเข้าวัตถุดิบ" ถูก **พอร์ตมาเกือบ 1:1** ระหว่างสองระบบ ทั้งลำดับกลุ่มและชื่อ field เกือบเหมือนกันทุกประการ:

| ลำดับกลุ่ม | Laravel เดิม | Next.js ใหม่ | เหมือน/ต่าง |
|---|---|---|---|
| 1 | ชื่อบริษัท / เลขที่ใบส่งสินค้า / วันที่ → ชนิดด้าย / ล็อตที่ | **ข้อมูลการนำเข้า**: ชื่อบริษัท / เลขที่ใบส่งสินค้า / วันที่ → ชนิดด้าย / ล็อตที่ / **พนักงาน** | เหมือนกันเกือบทั้งหมด — ใหม่เพิ่ม field "พนักงาน" เข้ามาในกลุ่มแรก (เดิมไม่มีในฟอร์มนี้) |
| 2 | หัวข้อ "บรรจุภัณฑ์": พาเลท+ประเภท / กล่อง / กระสอบ+ประเภท | **บรรจุภัณฑ์**: พาเลท+ประเภท / กล่อง / กระสอบ+ประเภท / กระดาษกั้น | เหมือนกัน — ระบบใหม่ย้าย "กระดาษกั้น" มารวมในกลุ่มนี้ (เดิมอยู่กลุ่มถัดไปปนกับจำนวนหลอด) |
| 3 | กระดาษกั้น + จำนวนหลอด + ประเภทหลอด + จำนวนด้ายทั้งหมด (รวมกลุ่มเดียว) | **จำนวน**: จำนวนหลอดทั้งหมด + ประเภทหลอด / จำนวนด้ายทั้งหมด | ระบบใหม่ **แยกกลุ่มชัดเจนกว่า** — "กระดาษกั้น" ย้ายไปกลุ่มบรรจุภัณฑ์ (ข้อ 2) เหลือกลุ่มนี้เฉพาะเรื่องจำนวน/หลอดด้าย ทำให้แต่ละ section มีความหมายเดียวชัดเจนขึ้น (เดิมปนกันระหว่าง "บรรจุภัณฑ์" กับ "จำนวน") |
| 4 | น้ำหนักรวม → น้ำหนักบรรจุภัณฑ์ → น้ำหนักสุทธิ → น้ำหนักเฉลี่ย/ลูก (คนละ label divider แยก 4 กลุ่มย่อย) | **น้ำหนัก**: น้ำหนักรวม → น้ำหนักบรรจุภัณฑ์ → น้ำหนักสุทธิ (คำนวณอัตโนมัติ) → น้ำหนักเฉลี่ย/ลูก (คำนวณอัตโนมัติ) — รวมเป็น**การ์ดเดียว** section เดียว | ระบบใหม่**รวม 4 กลุ่มย่อยเดิมเป็น section เดียว** ("น้ำหนัก") แทนที่จะแยก label divider 4 อัน — กระชับกว่า แต่ผู้ใช้เดิมที่คุ้นกับการเห็นหัวข้อแยกทีละหัวข้ออาจต้อง scan เยอะขึ้นใน section เดียว |
| 5 | เช็คบ๊อกซ์ "ส่งคืนบรรจุภัณฑ์" 5 ตัว | **ส่งคืนบรรจุภัณฑ์** (การ์ดสีเหลือง/amber) เช็คบ๊อกซ์ 5 ตัวเหมือนกัน | เหมือนกัน — แต่ ⚠️ ตามที่ระบุใน `comparison-raw-material.md` checkbox กลุ่มนี้เป็น UI-only ไม่ถูกส่งไป API จริง (ทั้งฟอร์ม create และ outside) |
| 6 | ไม่มี field หมายเหตุในฟอร์ม create วัตถุดิบ (มีเฉพาะ material_outsides) | **หมายเหตุ**: textarea เพิ่มใหม่ | field ใหม่ที่เดิมไม่มีในฟอร์มนำเข้า |

**สรุปการจัดกลุ่มฟอร์ม**: ระบบใหม่ **ไม่ได้ออกแบบใหม่ทั้งหมด** แต่ปรับปรุงจากโครงสร้างเดิมโดย (ก) รวมกลุ่มน้ำหนัก 4 กลุ่มย่อยเป็น 1 section, (ข) จัดกลุ่ม "บรรจุภัณฑ์" ให้ครบถ้วนกว่าเดิม (ย้ายกระดาษกั้นเข้ามา), (ค) เพิ่ม field พนักงาน+หมายเหตุที่เดิมไม่มี — ไม่มีการย้าย field ข้ามหน้า/step ใหม่ (ทั้งสองระบบเป็น single-page form เหมือนกัน ไม่มีระบบ tab/step ในฟอร์มนี้)

---

## 8. รูปแบบการ Submit ฟอร์ม — ความไม่สอดคล้องกันใหม่ที่ควรระวัง

ระบบใหม่มี 3 รูปแบบการ submit ที่ต่างกันในฟอร์มพี่น้องกันเอง (จุดที่ระบบเดิมไม่มีความไม่สอดคล้องแบบนี้ เพราะทุกฟอร์มเดิมใช้ pattern เดียวกันหมดคือ "กรอก→ตรวจสอบ→บันทึก"):

| ฟอร์ม | Laravel เดิม | Next.js ใหม่ |
|---|---|---|
| นำเข้าวัตถุดิบ (create) | กรอก → ตรวจสอบ (หน้า confirm) → บันทึก | กรอก → **บันทึกทันที** (single-submit, ไม่มีหน้ายืนยัน) |
| เบิกภายใน (requisition) | กรอกหลายแถว (JS เพิ่มแถวในฟอร์มเดียว) → ตรวจสอบ (หน้า confirm พร้อม lot allocation) → เบิกวัตถุดิบ (บันทึก) | กรอกทีละแถว → "+เพิ่มรายการใหม่" (เก็บ local pending list) → **"บันทึก N รายการ"** (ยิง API ทีเดียว, ไม่มีหน้า confirm แยก) |
| เบิกภายนอก (outside) | เหมือนแถวเดียวกับ material create (ตรวจสอบ→บันทึก) | เหมือน requisition ใหม่ (batch pending list → บันทึก N รายการ) |

**ระบบเดิม**: ทุกฟอร์มใช้ pattern เดียวกันทั้งหมด (2-step wizard: กรอก→ยืนยัน→บันทึก) — สอดคล้องกันทั้งระบบ ผู้ใช้เรียนรู้ครั้งเดียวใช้ได้ทุกฟอร์ม

**ระบบใหม่**: มี 2 pattern ต่างกัน — ฟอร์ม "นำเข้า" ใช้ single-submit ไม่มีหน้ายืนยัน ในขณะที่ "เบิกภายใน"/"เบิกภายนอก" ใช้ batch pending-list — ผู้ใช้ต้องเรียนรู้ 2 พฤติกรรมต่างกันสำหรับฟอร์มที่อยู่ในเมนูเดียวกัน ซึ่งเป็นจุดที่ควรพิจารณาทำให้สอดคล้องกันมากขึ้น (เช่น ให้ "นำเข้า" รองรับหลายแถวแบบเดียวกับอีกสองฟอร์ม หรือให้ทั้งสามฟอร์มมีขั้นตอนยืนยันก่อนบันทึกเหมือนกันหมด)

---

## 9. สรุปภาพรวม

| ประเด็น | สรุป |
|---|---|
| การจัดกลุ่มเมนูหลัก | ระบบใหม่ดีขึ้น — วัตถุดิบอยู่ติดกับคลังสินค้าโดยไม่มีเมนูตายคั่น |
| ลำดับเมนูย่อย | ระบบใหม่ดีขึ้น — จัดเป็นคู่ action→history ตาม workflow แทนที่จะแยกกลุ่ม index/create |
| ตำแหน่งเมนู "สต็อก" | ย้ายเข้ากลุ่มวัตถุดิบแล้ว (ดีขึ้นตาม concept) แต่ **มี permission bug ทำให้ non-admin เข้าถึงไม่ได้เลย** (แย่ลงในทางปฏิบัติ) — ควรแก้ไข `src/lib/menus.ts` โดยเพิ่ม `material.stock` เข้า `ALL_MENUS` |
| เมนู "คืนบรรจุภัณฑ์" | ⚠️ Label คล้ายเดิมแต่ชี้ไปหน้าอื่น (คืนผ้า) — ฟีเจอร์คืนบรรจุภัณฑ์วัตถุดิบของเดิมหายไปจาก UI ใหม่โดยสิ้นเชิง |
| ฟีเจอร์ใหม่ "คืนวัตถุดิบเข้าสต็อก" | Backend พร้อมแล้วแต่ **ไม่มี UI เลย** — เข้าถึงไม่ได้จากเมนูใดๆ ทั้งสิ้น ต้องสร้างหน้า/ปุ่มเมนูเพิ่ม |
| จำนวนคลิก | ระบบใหม่ใช้คลิกน้อยกว่าเดิมโดยรวม (ตัดหน้ายืนยันออกในฟอร์มนำเข้า) แต่แลกกับการไม่มีขั้นตอน preview ก่อนบันทึกจริง |
| การจัดกลุ่ม field ในฟอร์ม | พอร์ตมาเกือบ 1:1 จากเดิม ปรับปรุงเล็กน้อย (รวมกลุ่มน้ำหนักเป็น section เดียว, จัดกลุ่มบรรจุภัณฑ์ให้ครบขึ้น) ไม่มีการย้าย field ข้าม step/page |
| ความสอดคล้องของ submit pattern | ระบบเดิมสอดคล้องกันทุกฟอร์ม (wizard 2 ขั้นตอนเหมือนกันหมด) ระบบใหม่มี 2 pattern ต่างกันในฟอร์มพี่น้อง — ควรพิจารณาทำให้เป็นมาตรฐานเดียวกัน |
