# รายงานสำรวจโค้ด: ระบบ "คลังวัตถุดิบ" (Raw Material) — เตรียมสำหรับรีวิว UX

> สำรวจแบบ read-only จากโค้ดจริงใน `ast-new` (Next.js + Prisma/PostgreSQL) ณ วันที่ 2026-07-24
> ไม่มีการแก้ไขโค้ดใดๆ ในการสำรวจนี้ — เอกสารนี้มีไว้เพื่อส่งต่อให้ Claude อีกตัวช่วยรีวิว UX

---

## 0. ภาพรวมด่วน

- โดเมนวัตถุดิบมี 4 งานหลักที่ใช้งานได้จริงผ่านหน้าจอ: **นำเข้า (รับเข้า), เบิกภายใน, เบิกภายนอก, ดูสต็อกคงเหลือ**
- **ไม่มีฟีเจอร์ "ตรวจนับสต็อก" (physical stocktake)** ในระบบเลย ทั้งเดิมและใหม่ — มีแต่หน้า "ดูยอดคงเหลือ" ที่คำนวณสดจากผลรวม (รับเข้า − เบิก) ไม่ใช่การนับจริงแล้วบันทึกผลต่าง
- **"คืนวัตถุดิบเข้าสต็อก" มี API แต่ไม่มี UI เลย** — เข้าถึงไม่ได้จากเมนูใดๆ (endpoint `POST/GET /api/warehouse/material/return` มีจริงแต่ไม่มี `page.tsx` คู่กัน)
- ไม่มี UI library (ไม่ใช่ shadcn/MUI/Ant) — ทุกหน้าเขียน `<input>/<select>/<table>` ดิบด้วย Tailwind CSS เอง
- ไม่มีโค้ด Laravel Blade หลงเหลือใน repo นี้ (ตรวจสอบแล้ว ไม่มี `.blade.php` ทั้งใน working tree และ git log) — ระบบเดิมอยู่คนละ repo (`ast-laravel-clean`) จึงใช้ผลสำรวจเปรียบเทียบที่เคยทำไว้ก่อนหน้า (2026-07-10) มาอ้างอิงในหัวข้อ 4

---

## 1. รายการหน้า/Route ทั้งหมด

| งาน | Path | ไฟล์หลัก |
|---|---|---|
| นำเข้าวัตถุดิบ (create) | `/warehouse/material/create` | `MaterialCreateForm.tsx` |
| ประวัติการนำเข้า (list/edit/delete) | `/warehouse/material/history` | `MaterialHistoryList.tsx` |
| เบิกวัตถุดิบภายใน (create, multi-row) | `/warehouse/material/requisition` | `MaterialRequisitionForm.tsx` |
| ประวัติการเบิกภายใน | `/warehouse/material/requisition-history` | `RequisitionHistoryList.tsx` |
| เบิกวัตถุดิบภายนอก (create, multi-row) | `/warehouse/material/outside` | `MaterialOutsideForm.tsx` |
| ประวัติเบิกภายนอก | `/warehouse/material/outside-history` | `OutsideHistoryList.tsx` |
| สต็อกวัตถุดิบ (ยอดคงเหลือ) | `/warehouse/material/stock` | `MaterialStockList.tsx` |
| ~~คืนวัตถุดิบเข้าสต็อก~~ | ❌ ไม่มีหน้า (มีแค่ API) | `api/warehouse/material/return/route.ts` |
| ~~คืนบรรจุภัณฑ์ให้ซัพพลายเออร์~~ | เมนู "ส่งคืนบรรจุภัณฑ์" ชี้ไป `/warehouse/returns` แต่หน้านั้นคือหน้า **คืนผ้า** (`FabricReturnPage.tsx`) คนละโดเมน | — |

API endpoints เสริม (ไม่มีหน้า UI ของตัวเอง ใช้เป็น autocomplete): `/api/warehouse/material/lots`, `/suppliers`, `/yarn-types`, `/[id]`, `/entry`

เมนูทั้งหมดอยู่ใต้กลุ่ม sidebar เดียวกันชื่อ "ระบบวัตถุดิบ" (`src/app/(dashboard)/NavLinks.tsx:156-213`) เรียงลำดับ: นำเข้า → ประวัตินำเข้า → เบิกภายใน → ประวัติเบิก → เบิกภายนอก → ประวัติเบิกภายนอก → สต็อก → ส่งคืนบรรจุภัณฑ์(ผิดที่)

---

## 2. Flow แบบ step-by-step ของงานหลัก

### 2.1 นำเข้าวัตถุดิบ (รับเข้าคลัง)
1. คลิกขยายเมนู "ระบบวัตถุดิบ" → คลิก "นำเข้าวัตถุดิบ" (2 คลิก, 1 หน้า)
2. กรอกฟอร์มเดียวยาว (ไม่มี step/tab) แล้วกด "บันทึก" — **บันทึกทันที ไม่มีหน้ายืนยัน/preview**
3. **จำนวน field ในฟอร์ม: 29 fields** (แบ่งเป็น 6 กลุ่ม: ข้อมูลนำเข้า, บรรจุภัณฑ์, จำนวน, น้ำหนัก, ส่งคืนบรรจุภัณฑ์, หมายเหตุ) — 4 ใน 29 เป็น read-only auto-calculate (น้ำหนักสุทธิ/เฉลี่ย), 5 เป็น checkbox กลุ่ม "ส่งคืนบรรจุภัณฑ์"
4. รวม: **1 หน้า, 1 ฟอร์ม, 29 field, submit ครั้งเดียว**

⚠️ หมายเหตุ: checkbox กลุ่ม "ส่งคืนบรรจุภัณฑ์" (5 ตัว) และ field ประเภทบรรจุภัณฑ์ (palletType/sackType/spoolType/paperBar) เป็น **UI-only ไม่ถูกส่งไป API จริง** (ยืนยันจาก `comparison-raw-material.md`) — ผู้ใช้กรอกแต่ข้อมูลหายเงียบๆ

### 2.2 เบิกวัตถุดิบออก — ภายใน (requisition)
1. คลิกขยายเมนู → คลิก "เบิกวัตถุดิบภายใน" (2 คลิก, 1 หน้า)
2. กรอกฟอร์ม (10 field: แผนก, พนักงาน, บริษัท, ชนิดด้าย, ล็อต, จำนวนลูก, น้ำหนัก(lbs+kg), วันที่เบิก, หมายเหตุ) แล้วกด **"+ เพิ่มรายการใหม่"** → รายการถูกเก็บใน pending list บนหน้าเดียวกัน (ยังไม่ยิง API)
3. ทำซ้ำข้อ 2 ได้หลายรายการ (multi-row ในหน้าเดียว)
4. กด **"บันทึก N รายการ"** → ยิง API ครั้งเดียวทั้งชุด
5. รวม: **1 หน้า, ฟอร์มละ 10 field, รองรับหลายแถวก่อน submit จริง, ไม่มีหน้ายืนยันแยก**

พนักงาน (emp) ผูกกับ `localStorage` (`material-emp-list`) ไม่ใช่ API/DB จริง — ไม่ sync ข้ามเครื่อง/ผู้ใช้

### 2.3 เบิกวัตถุดิบออก — ภายนอก (outside/subcontract)
1. คลิกขยายเมนู → คลิก "เบิกวัตถุดิบภายนอก" (2 คลิก, 1 หน้า)
2. กรอกฟอร์ม (**22 field** — มากกว่าเบิกภายในกว่าเท่าตัว เพราะมี field เพิ่ม: บรรจุภัณฑ์/น้ำหนักรวม-บรรจุภัณฑ์-สุทธิ, checkbox คืนบรรจุภัณฑ์ 5 ตัว, ผู้รับ, การนำไปใช้, หมายเหตุการเงิน) → "+ เพิ่มรายการใหม่" → pending list → "บันทึก N รายการ"
3. รวม: **1 หน้า, ฟอร์มละ 22 field, รูปแบบ batch เหมือนเบิกภายใน แต่ field เยอะกว่ามาก**

### 2.4 ดูยอดคงเหลือ (stock)
1. คลิกขยายเมนู → คลิก "สต็อกวัตถุดิบ" (2 คลิก, 1 หน้า) — ตารางโหลดข้อมูลทั้งหมดทันทีโดยไม่ต้องกดค้นหา
2. มีช่องค้นหา 1 ช่อง (ค้นได้ทั้งชนิดด้าย/ชื่อบริษัทพร้อมกัน)
3. **ไม่มี pagination** — โหลดทุกแถวมาแสดงพร้อมกัน (ต่างจากหน้า history ที่มี pagination)
4. ⚠️ สูตรคำนวณคงเหลือหักลบเฉพาะเบิกภายใน ไม่หักเบิกภายนอกและไม่บวกคืน (ดูหัวข้อ 5)
5. ⚠️ เมนูนี้มี **permission bug**: `src/lib/menus.ts` `ALL_MENUS` ไม่มี key `material.stock` → ผู้ใช้ที่ไม่ใช่ admin ไม่มีทางถูกให้สิทธิ์เข้าดูหน้านี้ได้เลย

### 2.5 ตรวจนับสต็อก (stocktake)
❌ **ไม่มีฟีเจอร์นี้ในระบบทั้งเดิมและใหม่** — ไม่มีหน้า "นับของจริงแล้วปรับยอด" หรือบันทึกผลต่าง มีแต่หน้า 2.4 ที่เป็นยอดคำนวณสดอย่างเดียว

### 2.6 คืนวัตถุดิบเข้าสต็อก
❌ **ไปไม่ถึงเลยจาก UI** — 0 คลิกที่จะไปถึง เพราะไม่มีลิงก์เมนู/หน้าใดๆ ทั้งที่ backend (`/api/warehouse/material/return`) ทำงานได้จริง

---

## 3. Component หลัก / UI stack ต่อหน้า

**UI library ที่ใช้: ไม่มี** — ทุกหน้าเขียน native HTML element (`<input>`, `<select>`, `<table>`) + Tailwind CSS classes เอง ไม่มี shadcn/ui, MUI, Ant Design, Radix หรือ headless-UI ใดๆ ใน path ของโดเมนนี้ ไลบรารีเดียวที่ใช้คือ `@tanstack/react-query` สำหรับ data fetching (พบใน `MaterialStockList.tsx`, `MaterialHistoryList.tsx`)

| หน้า | Component หลักที่พบในโค้ด | ขนาดไฟล์ |
|---|---|---|
| นำเข้า (create) | custom `Field`/`SectionLabel` wrapper, native input/select, auto-calc on change (ไม่มี modal) | 523 บรรทัด |
| เบิกภายใน (requisition) | native input/select + pending-list table (แสดงในหน้าเดียวกัน ไม่ใช่ modal), localStorage-backed emp dropdown | 562 บรรทัด |
| เบิกภายนอก (outside) | เหมือนเบิกภายใน แต่ field มากกว่า, pending-list table | 691 บรรทัด (ไฟล์ใหญ่สุดในโดเมน) |
| ประวัตินำเข้า (history) | native `<table>` + pagination controls + **1 modal** (ใช้ร่วมกันทั้งดู/แก้ไข/ยืนยันลบ ผ่าน state `confirmDelete` สลับ view ภายใน modal เดียว) | 519 บรรทัด |
| ประวัติเบิกภายใน | เหมือนประวัตินำเข้า (table + pagination + 1 modal) | 557 บรรทัด |
| ประวัติเบิกภายนอก | เหมือนกัน แต่ไม่มี pagination controls ที่ตรวจพบ | 343 บรรทัด |
| สต็อก | native `<table>` ไม่มี pagination, summary cards (grid การ์ด 3 ใบ), search box เดียว, react-query สำหรับ auto-refetch | 171 บรรทัด (สั้นสุด) |

**ไม่มี modal ซ้อนกัน (nested modal)** ในทุกหน้าที่สำรวจ — modal ในหน้า history ใช้ตัวเดียวสลับ mode ภายใน ไม่ใช่เปิดซ้อน

---

## 4. เทียบกับ Laravel เดิม (จากผลสำรวจ 2026-07-10, repo `ast-laravel-clean`)

> โปรเจกต์นี้ (`ast-new`) ไม่มีโค้ด Blade เหลืออยู่เลย ตัวเลขด้านล่างอ้างอิงจากการอ่านโค้ดจริงของ repo เดิมที่ทำไว้ก่อนหน้า

| งาน | เดิม (Laravel) | ใหม่ (Next.js) | ต่างกันอย่างไร |
|---|---|---|---|
| นำเข้าวัตถุดิบ | **4 คลิก + 2 หน้า** (กรอก → หน้ายืนยัน/confirm → บันทึก) | **3 คลิก + 1 หน้า** (กรอก → บันทึกทันที) | ตัด confirm page ออก เร็วขึ้นแต่เสี่ยงกรอกผิดแล้วไม่รู้ตัวก่อน submit |
| เบิกภายใน | 4 คลิก + 2 หน้า, บันทึกทีละฟอร์ม (มีเพิ่มแถวได้ในฟอร์มเดียวผ่าน JS) พร้อม auto lot-allocation | 2 คลิกเข้าเพจ + batch pending-list ในหน้าเดียว + "บันทึก N รายการ" ครั้งเดียว, **ไม่มี auto lot-allocation** (ต้องเลือก lot เองทุกแถว) | รูปแบบ multi-row คล้ายกัน แต่ของใหม่ตัด auto-allocation logic ออก ผู้ใช้ต้องรู้ lot เองมากขึ้น |
| เบิกภายนอก | รูปแบบเดียวกับนำเข้า (confirm page), **ไม่รองรับหลายแถวต่อฟอร์ม** | batch pending-list เหมือนเบิกภายใน (รองรับหลายแถวแล้ว) แต่ field เยอะกว่าเดิมมาก | ใหม่ดีขึ้นเรื่อง multi-row แต่ฟอร์มยาวขึ้น |
| ดูสต็อกคงเหลือ | 3 คลิก แต่อยู่เมนู "ผู้ตรวจสอบ" แยกจากเมนูวัตถุดิบ (discoverability แย่) | 2 คลิก อยู่ในเมนูวัตถุดิบตรงๆ แต่ **non-admin เข้าไม่ได้เลยเพราะ permission bug** | ตำแหน่งดีขึ้นสำหรับ admin, แย่ลงสำหรับผู้ใช้ทั่วไป |
| คืนบรรจุภัณฑ์ให้ซัพพลายเออร์ | มีฟีเจอร์จริง (`package.index`) ผูกกับ 4 ตาราง track ยอดค้างคืน | เมนูชื่อคล้ายกัน ("ส่งคืนบรรจุภัณฑ์") แต่ชี้ไปหน้าคืนผ้าคนละโดเมน — ฟีเจอร์เดิมหายไปทั้งหมด | regression — ฟีเจอร์จริงหายไปจาก UI |
| คืนวัตถุดิบเข้าสต็อก | ไม่มีฟีเจอร์นี้เลย | มี backend แล้วแต่ 0 คลิกที่จะไปถึง (ไม่มี UI) | ฟีเจอร์ใหม่ที่สร้างไม่เสร็จ |
| Submit pattern โดยรวม | **สอดคล้องกันทุกฟอร์ม**: กรอก → ยืนยัน (confirm page) → บันทึก | **ไม่สอดคล้องกัน**: นำเข้า = single-submit ทันที / เบิกภายใน-ภายนอก = batch pending-list — ผู้ใช้ต้องเรียนรู้ 2 พฤติกรรมต่างกันในเมนูเดียวกัน | จุดที่ควรพิจารณาทำให้เป็นมาตรฐานเดียวกัน |

---

## 5. Pain point ที่สังเกตเห็นจากโค้ด

1. **ฟอร์มยาวผิดปกติ**: เบิกวัตถุดิบภายนอก 22 field และนำเข้าวัตถุดิบ 29 field ในหน้าเดียวไม่มี step/tab แบ่ง — เสี่ยง cognitive load สูง โดยเฉพาะเบิกภายนอกที่ field เกือบสองเท่าของเบิกภายใน (10 field) ทั้งที่ concept งานคล้ายกัน

2. **Field ที่กรอกแล้วข้อมูลหายเงียบๆ**: checkbox "ส่งคืนบรรจุภัณฑ์" (5 ตัว) และ field ประเภทบรรจุภัณฑ์ (palletType/sackType/spoolType/paperBar) ในทั้งฟอร์มนำเข้าและเบิกภายนอก เป็น UI-only ไม่ถูกส่งไป backend — ผู้ใช้กรอกโดยเข้าใจว่าถูกบันทึก แต่จริงๆ ไม่มีผลอะไรเลย เป็น pain point เชิง trust ที่ตรวจไม่พบจาก UI (ต้องอ่านโค้ด API ถึงจะรู้)

3. **ฟีเจอร์ที่มี backend แต่ไปไม่ถึง**: "คืนวัตถุดิบเข้าสต็อก" ทำงานได้ผ่าน API แต่ไม่มีทางเข้าถึงจาก UI เลย — งานเสร็จครึ่งเดียว

4. **Permission bug ทำให้ฟีเจอร์เข้าถึงไม่ได้**: หน้า "สต็อกวัตถุดิบ" ให้สิทธิ์ได้เฉพาะ admin เพราะ `menus.ts` ลืมใส่ key `material.stock` — ผู้ใช้ทั่วไปที่ต้องเช็คสต็อกก่อนเบิกจะเข้าหน้านี้ไม่ได้เลย ทั้งที่ตำแหน่งเมนูถูกออกแบบมาให้เข้าถึงง่ายแล้ว

5. **Label เมนูชี้ผิดหน้า**: "ส่งคืนบรรจุภัณฑ์" ในกลุ่มวัตถุดิบพาไปหน้าคืนผ้า (โดเมนคนละอัน) — ผู้ใช้คลิกด้วยความคาดหวังผิดทาง

6. **สูตรคำนวณสต็อกไม่ครบ**: หน้า "ดูยอดคงเหลือ" หักลบเฉพาะเบิกภายใน ไม่หักเบิกภายนอกและไม่บวกยอดคืน → ตัวเลขที่ผู้ใช้เห็นหน้าจอสูงกว่าความเป็นจริง เป็นความเสี่ยงเชิง trust ในข้อมูลที่แสดง ไม่ใช่แค่ UX

7. **Submit pattern ไม่สอดคล้องกันในเมนูเดียวกัน**: นำเข้า = กดบันทึกแล้วจบทันที (ไม่มี preview), เบิกภายใน/ภายนอก = ต้องกด "เพิ่มรายการ" หลายครั้งก่อนกด "บันทึก N รายการ" — สลับกันไปมาระหว่าง 3 หน้าที่อยู่เมนูเดียวกันอาจทำให้ผู้ใช้กดผิดขั้นตอน (เช่น คาดหวัง batch ในหน้านำเข้า หรือคาดหวัง single-submit ในหน้าเบิก)

8. **Emp/พนักงาน ผูกกับ localStorage ไม่ใช่ฐานข้อมูลจริง**: รายชื่อพนักงานในฟอร์มเบิกภายในเก็บใน `localStorage` (`material-emp-list`) ของเบราว์เซอร์ผู้ใช้แต่ละคน — ไม่ sync ข้ามเครื่อง/ผู้ใช้ ทั้งที่มี model `MaterialCoordinator` ใน schema อยู่แล้วแต่ไม่ได้ wire เป็น API จริง

9. **ไม่มี pagination ในหน้าสต็อก**: โหลดทุกแถวมาพร้อมกันในการ query เดียว ต่างจากหน้า history ที่มี pagination ครบ — ถ้าจำนวนชนิดวัตถุดิบเพิ่มขึ้นมากจะกระทบ perf/scroll experience

10. **Modal เดียวทำ 3 หน้าที่** (ดู/แก้ไข/ยืนยันลบ) ในหน้าประวัติทั้งสาม — ไม่ใช่ modal ซ้อนกันแบบเป็นปัญหาหนัก แต่การสลับ mode ภายใน modal เดียวกัน (toggle `confirmDelete`) อาจทำให้ผู้ใช้สับสนว่ากำลังอยู่ในโหมดไหนถ้าไม่มี visual cue ชัดเจนพอ (ควรตรวจสอบด้วยตาจริงเพิ่มเติม)

---

## แหล่งอ้างอิงเพิ่มเติมในโปรเจกต์
- `comparison-raw-material.md` — เปรียบเทียบ schema/business-logic ฉบับเต็ม (field-by-field)
- `comparison-menu-structure.md` — เปรียบเทียบโครงสร้างเมนู/navigation ฉบับเต็ม
